
<li class="micropost">
 
  <div class="gravatar">
    <%= link_to gravatar_for(feed_item.user), feed_item.user %>
  </div>

 <%= render :partial => 'microposts/delete', :locals => { :post => feed_item } %>

 <%= render :partial => 'microposts/content', :locals => { :post =>  feed_item } %>

 <%= render :partial => 'shared/feed_item', :collection => feed_item.replies %>

</li>

