jQuery(function($) {
    $("#follow_form")
	.bind('ajax:complete', function() {
	    $("#follow_form").replaceWith("<%= escape_javascript(render('users/follow')) %>")
	    $("#followers").replaceWith('<%= "#{@user.followers.count} followers" %>');
	});
});

// Prototype.js version - works fine :)
//$("follow_form").update("<%= escape_javascript(render('users/follow')) %>")
//$("followers").update('<%= "#{@user.followers.count} followers" %>')